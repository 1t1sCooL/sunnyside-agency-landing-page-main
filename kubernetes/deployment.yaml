apiVersion: apps/v1
kind: Deployment
metadata:
  name: sunnyside-agency-landing-page-main-app
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sunnyside-agency-landing-page-main-app
  template:
    metadata:
      labels:
        app: sunnyside-agency-landing-page-main-app
    spec:
      containers:
        - name: sunnyside-agency-landing-page-main-app
          image: sunnyside-agency-landing-page-main-app:latest
          ports:
            - containerPort: 80
          env:
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
